# Placeholder “runbook glue” file.
#
# Purpose: keep at least one repo-tracked health.d file so operators have
# a canonical copy path and can extend with log-based alarms (e.g. watchtower,
# breaker state changes) without editing `/opt/netdata/...` blindly.
#
# NOTE: This file intentionally defines no active alarms yet. The active custom
# alarms are documented in `docs/NETDATA-ACCESS.md` and live on the server.

